# Sentiment Flow Editor runs entirely in the browser. LLM features use the
# OpenAI API key each user enters in the Settings modal -- not an env var.
#
# The only env var the app reads at build time is the optional site URL used
# for social-share metadata. Leave it unset for local dev.
NEXT_PUBLIC_SITE_URL=
